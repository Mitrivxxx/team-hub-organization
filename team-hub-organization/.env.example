# Local `dotnet run` (docker-compose.dev.yml → team-hub-dev):
ConnectionStrings__DefaultConnection=Host=localhost;Port=5433;Database=organization_db;Username=matsma
Jwt__Key=your-secret-key-at-least-32-characters
Jwt__Issuer=AuthService
Jwt__Audience=AuthServiceUsers
# Docker compose prod (team container → postgres service):
# ConnectionStrings__DefaultConnection=Host=postgres;Port=5432;Database=organizationdb;Username=matsma
